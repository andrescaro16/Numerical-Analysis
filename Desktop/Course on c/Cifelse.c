#include <stdio.h>

//Conditional statements learning :)

// i will be writting some complicated code checking out some inputs and outputs as well as using some conditional statements and logical operators.
//very very simple stuff
int main(){

    int tobe;
    printf("\n Enter an integer: ");
    scanf("%d", &tobe);  
    printf("\n Number = %d",tobe);

    if(tobe < 90){
        
        printf("\t This works");   
        return 0; 
    }
    else{
    }   
        printf("\t this does not work ");


}